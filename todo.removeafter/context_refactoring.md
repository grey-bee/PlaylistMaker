# Контекст рефакторинга PlaylistMaker на MVVM

## Дата обновления: 2025-12-27

## Что уже сделано ✅

1. **CLAUDE.md обновлён** — добавлено правило №5 о запрете готового кода
2. **Main экран рефакторен:**
   - Перенесён в `com.practicum.playlistmaker.main.ui.MainActivity`
   - Добавлен ViewBinding
   - Упрощён код (убраны findViewById, оптимизированы Intent)
   - AndroidManifest автоматически обновился
   - ViewModel не создавалась (там только навигация, нет логики)
3. **Settings полностью готов** ✅ (пользователь сделал сам)
4. **Search в процессе работы:**
   - Перенесён в `com.practicum.playlistmaker.search.ui/`
   - Создана SearchViewModel с debounce логикой
   - Создан SearchScreenState (sealed class) для состояний
   - Реализована LiveData для состояний экрана
   - Activity подписывается на состояния через observe
   - Правильно обработаны все состояния (Loading, Content, Empty, Error)

## Текущий статус TODO:

- [✅] Закоммитить текущие незавершенные изменения в Settings
- [✅] Рефакторинг Main на MVVM (по фичам)
- [✅] Рефакторинг Settings на MVVM (по фичам)
- [🔄] Рефакторинг Search на MVVM (по фичам) **← ТЕКУЩАЯ ЗАДАЧА**
- [ ] Рефакторинг Sharing на MVVM (по фичам)
- [ ] Рефакторинг Player на MVVM (по фичам)
- [ ] Рефакторинг MediaLibrary на MVVM (по фичам)
- [ ] Проверка работы всего приложения после рефакторинга

## Целевая структура проекта (по фичам):

```
com.practicum.playlistmaker/
├── main/
│   └── ui/
│       └── MainActivity.kt ✅ ГОТОВО
├── settings/
│   ├── data/
│   ├── domain/
│   └── ui/ ✅ ГОТОВО
├── sharing/
│   ├── data/
│   ├── domain/
│   └── ui/
├── search/
│   ├── data/
│   ├── domain/
│   └── ui/ 🔄 В ПРОЦЕССЕ
├── player/
│   ├── data/
│   ├── domain/
│   └── ui/
└── medialibrary/
    ├── data/
    ├── domain/
    └── ui/
```

## Детали реализации Search (текущее состояние):

### Структура файлов:
```
search/
├── domain/
│   ├── api/
│   │   └── SearchTracksInteractor.kt (интерфейс с методом invoke)
│   └── model/
│       └── Track.kt
└── ui/
    ├── SearchActivity.kt
    ├── SearchViewModel.kt
    └── SearchScreenState.kt (sealed interface)
```

### SearchScreenState (sealed class для состояний):
```kotlin
sealed interface SearchScreenState {
    object Loading : SearchScreenState
    data class Content(val tracks: List<Track>) : SearchScreenState
    object Error : SearchScreenState
    object Empty : SearchScreenState
}
```

**Важно:**
- `object` публикуется без скобок: `SearchScreenState.Loading`
- `data class` публикуется с данными: `SearchScreenState.Content(list)`

### SearchViewModel - ключевые моменты:

**Что есть:**
1. Handler для debounce (2 секунды задержки)
2. LiveData<SearchScreenState> для публикации состояний
3. Метод `onTextChanged()` - вызывается из TextWatcher в Activity
4. Метод `searching()` - выполняет поиск через interactor
5. `onCleared()` - очищает Handler

**Логика обработки результатов:**
```kotlin
private fun searching() {
    stateLiveData.postValue(SearchScreenState.Loading)  // показываем прогресс
    searchTracksInteractor(currentSearchText, callback) {
        if (foundTracks == null) {
            // Ошибка сети
            stateLiveData.postValue(SearchScreenState.Error)
        } else if (foundTracks.isEmpty()) {
            // Успешный поиск, но ничего не найдено
            stateLiveData.postValue(SearchScreenState.Empty)
        } else {
            // Есть результаты
            stateLiveData.postValue(SearchScreenState.Content(foundTracks))
        }
    }
}
```

**Важные различия:**
- `null` = ошибка сети/соединения → Error
- пустой список `[]` = успешный поиск, ничего не нашли → Empty

### SearchActivity - ключевые моменты:

**Что есть:**
1. ViewBinding
2. ViewModel создаётся через `by viewModels()`
3. TrackAdapter для отображения результатов
4. TextWatcher следит за полем ввода (остаётся в UI!)
5. Подписка на LiveData из ViewModel

**Подписка на состояния:**
```kotlin
viewModel.observeState().observe(this) { state ->
    when (state) {
        is SearchScreenState.Loading -> showLoading()
        is SearchScreenState.Content -> showContent(state.tracks)
        is SearchScreenState.Empty -> showNothingFound()
        is SearchScreenState.Error -> showError()
    }
}
```

**Методы для отображения (UI слой):**
- `showLoading()` - показывает прогресс, прячет всё остальное
- `showContent(tracks)` - обновляет адаптер, показывает список
- `showNothingFound()` - показывает экран "Nothing found"
- `showError()` - показывает экран "No connection"

## Важные концепции MVVM (усвоенные):

### 1. Разделение ответственности:

**ViewModel отвечает за:**
- Хранение данных (списки, состояния)
- Бизнес-логику (debounce, когда искать)
- Вызов interactor'ов
- Публикацию состояния через LiveData
- НЕ ЗНАЕТ про View (никаких `isVisible`, `binding` и т.д.)

**Activity отвечает за:**
- Отображение данных (показать/скрыть view)
- TextWatcher и другие UI слушатели
- Подписку на LiveData
- Обновление адаптеров
- НЕ СОДЕРЖИТ бизнес-логику

### 2. TextWatcher остаётся в Activity:
- Это UI-компонент, работает с View
- НО вместо прямого вызова логики → вызывает метод ViewModel
- ViewModel решает что делать дальше

### 3. Адаптеры - это UI слой:
- Создаются и хранятся в Activity
- ViewModel НЕ знает про адаптеры
- ViewModel публикует данные → Activity обновляет адаптер

### 4. Handler для debounce:
- Handler НЕ создаёт новый поток (это UI thread)
- Handler откладывает выполнение кода
- Handler должен быть в ViewModel как свойство экземпляра (НЕ в companion!)
- В `onCleared()` нужно очистить Handler

### 5. Sealed class для состояний экрана:
- **Это UI-модель** (держим в `ui/`, НЕ в `domain/model/`)
- Описывает КАК показывать экран, а не бизнес-сущности
- Лучше чем несколько отдельных LiveData
- `object` для простых состояний, `data class` для состояний с данными

### 6. Публикация состояний:
- `stateLiveData.postValue(SearchScreenState.Loading)` - для object
- `stateLiveData.postValue(SearchScreenState.Content(list))` - для data class
- НЕ нужны скобки для object!

## Best practices для структуры Activity:

```kotlin
class SomeActivity {
    // 1. Properties (поля)
    private lateinit var binding: ...
    private val viewModel: ... by viewModels()
    private val adapter = ...

    // 2. Lifecycle методы (в порядке жизненного цикла!)
    override fun onCreate() { }
    override fun onDestroy() { }
    override fun onSaveInstanceState() { }
    override fun onRestoreInstanceState() { }

    // 3. Private методы (вспомогательные)
    private fun showSomething() { }

    // 4. Companion object
    companion object { }
}
```

## Что осталось для Search:

1. **История поиска** (закомментирована):
   - `getSearchHistoryInteractor`
   - `clearSearchHistoryInteractor`
   - `addTrackToHistoryInteractor`
   - Нужно реализовать в ViewModel

2. **Click debounce** (закомментирован):
   - Защита от двойных кликов на треках
   - Перенести логику в ViewModel

3. **Навигация в Player** (закомментирована):
   - `openAudioPlayer(track)`
   - Добавление в историю при клике

4. **RefreshButton**:
   - Сейчас только показывает Loading
   - Нужно вызвать повторный поиск через ViewModel

5. **Тестирование**:
   - Проверить Loading → Content
   - Проверить Empty (ничего не найдено)
   - Проверить Error (нет интернета)

## Следующие действия:

1. Доделать Search (история, клики, навигация)
2. Протестировать все состояния Search
3. Закоммитить Search
4. Переходить к Sharing
5. Player
6. MediaLibrary
7. Финальное тестирование всего приложения

## Важные замечания:

1. **Проблема с импортами** — после незавершённого рефакторинга много файлов с неправильными импортами. Решено: исправлен SharingInteractorImpl (импортировал ExternalNavigator из data вместо domain).

2. **Чистая архитектура:**
   - Domain НЕ зависит от Data
   - Domain использует только интерфейсы
   - Data реализует интерфейсы из Domain
   - UI-модели состояний НЕ относятся к domain

3. **После каждого шага** делать Build (Ctrl+F9) и проверять что нет ошибок компиляции

4. **Коммитить** после каждого успешно завершённого экрана
